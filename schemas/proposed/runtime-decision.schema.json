{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openinstitutionalcompiler.org/schemas/proposed/runtime-decision.schema.json",
  "title": "RuntimeDecision",
  "description": "PROPOSED, NOT ADMITTED. Separates what the grounds establish (epistemic_status), what the runtime did (execution_disposition), and why it did it (decision_basis). schemas/draft/runtime-evaluation.schema.json collapses all three into a single `verdict` field; that collapse is the defect this schema exists to correct. The cross-field constraints in `allOf` are the contract, not documentation: a decision violating one is invalid, not merely discouraged. See adr/ADR-013.md. schemas/draft is NOT modified by this proposal.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "evaluation_id",
    "envelope_id",
    "control_id",
    "control_version",
    "epistemic_status",
    "execution_disposition",
    "decision_basis",
    "reason_codes",
    "missing_ground_ids",
    "missing_ground_anchors",
    "warrant_artifact_id",
    "warrant_state",
    "warranty_grade_observed",
    "warranty_grade_required",
    "on_unknown_applied",
    "replay_reference",
    "authority_version",
    "admission_version",
    "source_version_set_hash",
    "input_hash",
    "evaluated_at",
    "unverified_ground_policy_applied"
  ],
  "$defs": {
    "sha256_digest": {
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$"
    },
    "identifier": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256
    },
    "reason_code": {
      "type": "string",
      "pattern": "^OIC-[WD]-[0-9]{4}$",
      "description": "Stable machine-readable reason. Registry in docs/contracts/WARRANT-CONTRACT-v0.1.md."
    },
    "control_policy_reason_code": {
      "$comment": "Reason codes that justify a CONTROL_REQUIREMENT basis.",
      "enum": [
        "OIC-W-0016",
        "OIC-D-0002",
        "OIC-D-0003",
        "OIC-D-0005",
        "OIC-D-0006"
      ]
    }
  },
  "properties": {
    "evaluation_id": {
      "$ref": "#/$defs/identifier"
    },
    "envelope_id": {
      "$ref": "#/$defs/identifier"
    },
    "control_id": {
      "$ref": "#/$defs/identifier"
    },
    "control_version": {
      "$ref": "#/$defs/identifier"
    },
    "epistemic_status": {
      "enum": [
        "ESTABLISHED",
        "CONDITIONALLY_SUPPORTED",
        "REFUTED",
        "UNRESOLVED",
        "CONTRADICTED"
      ],
      "description": "What the admitted grounds establish about the claim. ESTABLISHED: the grounds establish it independently of any unresolved ground, under the profile's hereditary contract. CONDITIONALLY_SUPPORTED: the current admitted marking supports the claim, but the positive result depends on one or more unverified grounds and is not invariant under admissible refinement. REFUTED: the grounds establish its negation. UNRESOLVED: the grounds do not settle it, or it was never validly evaluated. CONTRADICTED: the admitted ground set is inconsistent, which is a finding about the grounds rather than a refutation of the proposition. ON CREDIT MUST NEVER serialise as ESTABLISHED: that would authorise action on an unverified link. Nor may it collapse to UNRESOLVED merely because grounds are unverified, which would discard a real positive result."
    },
    "execution_disposition": {
      "enum": [
        "ALLOW",
        "BLOCK",
        "ESCALATE",
        "ADVISORY"
      ],
      "description": "What the runtime did. Independent of epistemic_status: a BLOCK may accompany UNRESOLVED (fail closed) or REFUTED (grounds establish the negation), and those are not the same event."
    },
    "decision_basis": {
      "enum": [
        "SUBSTANTIVE",
        "PRECAUTIONARY",
        "PROCEDURAL",
        "CONTROL_REQUIREMENT"
      ],
      "description": "Why the runtime did it. SUBSTANTIVE: the admitted grounds determine a positive, negative, or contradictory epistemic result. PRECAUTIONARY: the grounds remain unresolved and the runtime fails closed. PROCEDURAL: the claim could not be validly evaluated because an artifact, binding, version, availability, or integrity precondition failed. CONTROL_REQUIREMENT: the claim WAS validly evaluated, but the admitted control requires a particular assurance grade, human role, escalation path, or non-automatic decision mode. Only SUBSTANTIVE is a finding about the world; PRECAUTIONARY, PROCEDURAL and CONTROL_REQUIREMENT are never findings that the proposition is false."
    },
    "reason_codes": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/reason_code"
      },
      "minItems": 1,
      "description": "Sorted lexicographically, deterministic. At least one is always present, including on ALLOW."
    },
    "missing_ground_ids": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/identifier"
      },
      "description": "Level 1: stable proposition or atom identifiers as emitted by the kernel. Present whenever the kernel reported unverified grounds, INCLUDING when the outcome is ALLOW."
    },
    "missing_ground_anchors": {
      "type": "array",
      "description": "Level 2: human-review references linking each missing proposition to its source and admission context. Enrichment is the OIC adapter's job, never the kernel's. An empty list means enrichment has not run; it does not mean there are no missing grounds.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "ground_id"
        ],
        "properties": {
          "ground_id": {
            "$ref": "#/$defs/identifier"
          },
          "source_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "source_node_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "span": {
            "type": [
              "object",
              "null"
            ],
            "additionalProperties": false,
            "properties": {
              "start": {
                "type": "integer",
                "minimum": 0
              },
              "end": {
                "type": "integer",
                "minimum": 0
              }
            }
          },
          "admitted_unit_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "expected_evidence_type": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      }
    },
    "warrant_artifact_id": {
      "type": [
        "string",
        "null"
      ],
      "description": "The warrant this decision rests on, or null when no usable warrant existed. Null with ALLOW is forbidden by the constraints below."
    },
    "warrant_state": {
      "enum": [
        "USABLE",
        "ABSENT",
        "MALFORMED",
        "UNVERIFIABLE",
        "STALE",
        "EXPIRED",
        "NOT_YET_VALID",
        "REVOKED",
        "MISBOUND",
        "UNSUPPORTED_GRADE",
        "NOT_REQUIRED"
      ],
      "description": "Why the warrant was or was not usable. MISBOUND covers epoch, formula, profile, source-version, and admission-version mismatch: the artifact is well formed but describes a different world, a different proposition, or a different kernel's semantics. NOT_REQUIRED applies only when the control's warrant_requirement.mode is not_required or not_applicable, and never waives authority, admission, evidence, or version checks."
    },
    "warranty_grade_observed": {
      "type": [
        "string",
        "null"
      ],
      "enum": [
        "hereditary",
        "sound",
        "until-verification",
        null
      ],
      "description": "Grade the warrant actually carried. Null only when no warrant was evaluated."
    },
    "warranty_grade_required": {
      "type": [
        "string",
        "null"
      ],
      "enum": [
        "hereditary",
        "sound",
        "until-verification",
        null
      ],
      "description": "Minimum grade the control's warrant_requirement declares. Null only when warrant_requirement.mode is not_required or not_applicable. There is no default: an absent requirement is a contract error, not a permissive one."
    },
    "on_unknown_applied": {
      "type": [
        "string",
        "null"
      ],
      "enum": [
        "cannot",
        "escalate",
        "deny",
        null
      ],
      "description": "The envelope's on_unknown policy, set ONLY when it was the reason for the outcome, otherwise null. `deny` means an operational fail-closed block and is never evidence that the proposition is false; the constraints below forbid pairing it with SUBSTANTIVE or ALLOW."
    },
    "replay_reference": {
      "type": "string",
      "minLength": 1,
      "description": "How to replay this decision to its exact control, admission, sources, warrant, inputs, and versions (I-15). REQUIRED and non-empty on EVERY decision including failures: an evaluation that blocked because no warrant existed must still be replayable, or the failure cannot be audited."
    },
    "authority_version": {
      "$ref": "#/$defs/identifier"
    },
    "admission_version": {
      "$ref": "#/$defs/identifier"
    },
    "source_version_set_hash": {
      "$ref": "#/$defs/sha256_digest"
    },
    "input_hash": {
      "$ref": "#/$defs/sha256_digest"
    },
    "evaluated_at": {
      "type": "string",
      "format": "date-time"
    },
    "unverified_ground_policy_applied": {
      "type": [
        "string",
        "null"
      ],
      "enum": [
        "forbid",
        "escalate",
        "allow_with_disclosure",
        null
      ],
      "description": "The control's unverified_ground_policy, recorded when it determined the outcome, otherwise null. Permission to act on conditional support is an admitted control decision, not a stronger epistemic claim, so the policy that granted it stays on the record."
    }
  },
  "allOf": [
    {
      "$comment": "Rule 1. ALLOW has exactly two positive shapes. Route A: the EARNED/hereditary route, ESTABLISHED + SUBSTANTIVE. Route B: the ON CREDIT/sound route, CONDITIONALLY_SUPPORTED + CONTROL_REQUIREMENT with allow_with_disclosure, OIC-D-0005, and non-empty missing grounds. Both require a USABLE warrant, a non-null warrant id, and no on_unknown fallback.",
      "if": {
        "properties": {
          "execution_disposition": {
            "const": "ALLOW"
          }
        },
        "required": [
          "execution_disposition"
        ]
      },
      "then": {
        "properties": {
          "warrant_state": {
            "const": "USABLE"
          },
          "warrant_artifact_id": {
            "type": "string",
            "minLength": 1
          },
          "on_unknown_applied": {
            "type": "null"
          }
        },
        "oneOf": [
          {
            "$comment": "Route A - EARNED / hereditary.",
            "properties": {
              "epistemic_status": {
                "const": "ESTABLISHED"
              },
              "decision_basis": {
                "const": "SUBSTANTIVE"
              },
              "warranty_grade_observed": {
                "const": "hereditary"
              }
            },
            "required": [
              "epistemic_status",
              "decision_basis"
            ]
          },
          {
            "$comment": "Route B - ON CREDIT / sound, accepted by explicit control policy.",
            "properties": {
              "epistemic_status": {
                "const": "CONDITIONALLY_SUPPORTED"
              },
              "decision_basis": {
                "const": "CONTROL_REQUIREMENT"
              },
              "warranty_grade_observed": {
                "const": "sound"
              },
              "unverified_ground_policy_applied": {
                "const": "allow_with_disclosure"
              },
              "missing_ground_ids": {
                "type": "array",
                "minItems": 1
              },
              "reason_codes": {
                "type": "array",
                "contains": {
                  "const": "OIC-D-0005"
                }
              }
            },
            "required": [
              "epistemic_status",
              "decision_basis",
              "unverified_ground_policy_applied"
            ]
          }
        ]
      }
    },
    {
      "$comment": "Rule 2. UNRESOLVED can neither ALLOW nor claim a substantive finding.",
      "if": {
        "properties": {
          "epistemic_status": {
            "const": "UNRESOLVED"
          }
        },
        "required": [
          "epistemic_status"
        ]
      },
      "then": {
        "properties": {
          "execution_disposition": {
            "enum": [
              "BLOCK",
              "ESCALATE",
              "ADVISORY"
            ]
          },
          "decision_basis": {
            "enum": [
              "PRECAUTIONARY",
              "PROCEDURAL",
              "CONTROL_REQUIREMENT"
            ]
          }
        }
      }
    },
    {
      "$comment": "Rule 3. CONDITIONALLY_SUPPORTED: never substantive, never without missing grounds, never with an on_unknown fallback, and never ALLOW on the weakest grade. v0.1 does not model risk, reversibility, compensation, or recovery controls sufficient to permit automatic consequential ALLOW on until-verification.",
      "if": {
        "properties": {
          "epistemic_status": {
            "const": "CONDITIONALLY_SUPPORTED"
          }
        },
        "required": [
          "epistemic_status"
        ]
      },
      "then": {
        "properties": {
          "decision_basis": {
            "enum": [
              "PRECAUTIONARY",
              "CONTROL_REQUIREMENT"
            ]
          },
          "missing_ground_ids": {
            "type": "array",
            "minItems": 1
          },
          "on_unknown_applied": {
            "type": "null"
          },
          "warranty_grade_observed": {
            "enum": [
              "sound",
              "until-verification"
            ]
          }
        },
        "not": {
          "properties": {
            "execution_disposition": {
              "const": "ALLOW"
            },
            "warranty_grade_observed": {
              "const": "until-verification"
            }
          },
          "required": [
            "execution_disposition",
            "warranty_grade_observed"
          ]
        }
      }
    },
    {
      "$comment": "Rule 4. REFUTED never permits the action. It blocks substantively, or is redirected by a control overlay which preserves the epistemic status.",
      "if": {
        "properties": {
          "epistemic_status": {
            "const": "REFUTED"
          }
        },
        "required": [
          "epistemic_status"
        ]
      },
      "then": {
        "properties": {
          "execution_disposition": {
            "enum": [
              "BLOCK",
              "ESCALATE",
              "ADVISORY"
            ]
          },
          "decision_basis": {
            "enum": [
              "SUBSTANTIVE",
              "CONTROL_REQUIREMENT"
            ]
          }
        }
      }
    },
    {
      "$comment": "Rule 5. CONTRADICTED behaves like REFUTED for execution: a finding about the ground set, never a permission.",
      "if": {
        "properties": {
          "epistemic_status": {
            "const": "CONTRADICTED"
          }
        },
        "required": [
          "epistemic_status"
        ]
      },
      "then": {
        "properties": {
          "execution_disposition": {
            "enum": [
              "BLOCK",
              "ESCALATE",
              "ADVISORY"
            ]
          },
          "decision_basis": {
            "enum": [
              "SUBSTANTIVE",
              "CONTROL_REQUIREMENT"
            ]
          }
        }
      }
    },
    {
      "$comment": "Rule 6. Any non-USABLE warrant state cannot ALLOW.",
      "if": {
        "properties": {
          "warrant_state": {
            "enum": [
              "ABSENT",
              "MALFORMED",
              "UNVERIFIABLE",
              "STALE",
              "EXPIRED",
              "NOT_YET_VALID",
              "REVOKED",
              "MISBOUND",
              "UNSUPPORTED_GRADE",
              "NOT_REQUIRED"
            ]
          }
        },
        "required": [
          "warrant_state"
        ]
      },
      "then": {
        "properties": {
          "execution_disposition": {
            "enum": [
              "BLOCK",
              "ESCALATE",
              "ADVISORY"
            ]
          }
        }
      }
    },
    {
      "$comment": "Rule 7. A non-null on_unknown fallback cannot ALLOW and cannot be substantive.",
      "if": {
        "properties": {
          "on_unknown_applied": {
            "type": "string"
          }
        },
        "required": [
          "on_unknown_applied"
        ]
      },
      "then": {
        "properties": {
          "execution_disposition": {
            "enum": [
              "BLOCK",
              "ESCALATE",
              "ADVISORY"
            ]
          },
          "decision_basis": {
            "enum": [
              "PRECAUTIONARY",
              "PROCEDURAL",
              "CONTROL_REQUIREMENT"
            ]
          }
        }
      }
    },
    {
      "$comment": "Rule 8. CONTROL_REQUIREMENT must carry a control-policy reason code. It never rewrites the epistemic status; a control overlay changes what the runtime did, not what the grounds establish.",
      "if": {
        "properties": {
          "decision_basis": {
            "const": "CONTROL_REQUIREMENT"
          }
        },
        "required": [
          "decision_basis"
        ]
      },
      "then": {
        "properties": {
          "reason_codes": {
            "type": "array",
            "contains": {
              "$ref": "#/$defs/control_policy_reason_code"
            }
          }
        }
      }
    },
    {
      "$comment": "Rule 9. allow_with_disclosure is only meaningful for conditional support.",
      "if": {
        "properties": {
          "unverified_ground_policy_applied": {
            "const": "allow_with_disclosure"
          }
        },
        "required": [
          "unverified_ground_policy_applied"
        ]
      },
      "then": {
        "properties": {
          "epistemic_status": {
            "const": "CONDITIONALLY_SUPPORTED"
          },
          "missing_ground_ids": {
            "type": "array",
            "minItems": 1
          }
        }
      }
    }
  ]
}
