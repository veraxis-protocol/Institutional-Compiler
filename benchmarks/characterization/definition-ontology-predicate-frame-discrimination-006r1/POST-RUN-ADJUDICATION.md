# Ontology 006R1 — Post-Run Scientific Adjudication

**Final classification:** `REFUTES_PREDICATE_FRAME_ROLE_GUIDANCE`

## Execution integrity

Ontology 006R1 executed exactly once.

The run was fully adjudicable:

- 18/18 frozen semantic requests accepted;
- 9/9 baseline/treatment pairs complete;
- 18 transport calls observed;
- transport ceiling: 36;
- provider errors: zero;
- boundary rejections: zero.

The repaired target preserved the frozen Ontology 006 semantic experiment:

- 18/18 semantic request projections byte-identical;
- semantic execution delegated directly to frozen Ontology 006;
- semantic decision rule delegated directly to frozen Ontology 006;
- transport policy delegated directly to frozen Ontology 006.

## Frozen hypothesis

The experiment tested whether appending only the already-frozen Institutional
IR action/object role descriptions to the otherwise frozen B2 non-force prompt
would restore the governing IIR-027 action predicate without introducing new
non-force defects.

## Frozen decision rule

`REFUTES_PREDICATE_FRAME_ROLE_GUIDANCE` requires:

- no role-guided-only safety regression;
- role-guided IIR-027 action compatibility at most 1/3;
- zero paired improvements.

## Observed target endpoint

For IIR-027 / action:

- expected governing value: `consider`;
- role-guided compatible: **0/3**;
- paired improvements: **0/3**.

The role-guided arm returned `reconciliation` in all three target runs.

The treatment therefore did not restore the governing predicate.

## Safety endpoint

Role-guided-only defects: **0**.

The treatment did not trigger the preregistered safety-regression rule.

## Scientific conclusion

The preregistered predicate-frame role-guidance hypothesis is **refuted within
the frozen scope**.

Restoring only the already-frozen action/object role descriptions is not
sufficient to repair the localized IIR-027 predicate-frame failure.

This is a negative result about this specific intervention.

It does **not** establish why the predicate-frame failure occurs.

The underlying cause remains **NOT ESTABLISHED**.

## Consequences

- O006R1 scientific disposition:
  `REFUTES_PREDICATE_FRAME_ROLE_GUIDANCE`
- O006R1 rerun: **NOT AUTHORIZED**
- semantic result established: **YES, bounded**
- provider/boundary failure: **NO**
- canonicalization performed: **NO**
- Institutional IR constructed: **NO**
- architecture change authorized: **NO**
- independent validation claimed: **NO**
- successor experiment automatically authorized: **NO**

## Claim ceiling

This result establishes only bounded evidence that the tested addition of the
already-frozen action/object role descriptions does not repair the localized
IIR-027 predicate-frame failure for this one model/provider and three frozen
synthetic specimens.

It does not establish:

- root cause;
- canonical institutional meaning;
- interpretation authority;
- legal validity;
- a revised IR ontology;
- production architecture;
- cross-model/provider generalization;
- independent validation.

## Next activity

First independently verify this closure commit.

Only after that verification may a fresh bounded diagnostic hypothesis be
designed and separately preregistered.

Do not rerun O006R1.
