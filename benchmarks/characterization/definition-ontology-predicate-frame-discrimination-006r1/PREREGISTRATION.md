# OIC Definition Ontology Predicate-Frame Discrimination 006R1 — Preregistration

Status: **PREREGISTERED AUTHORIZATION-BINDING REPAIR / NOT IMPLEMENTED / NOT EXECUTED**

## Why this revision exists

The original frozen Ontology 006 semantic experiment remains scientifically
valid and unexecuted.

A later formally closed Provider Qualification 007 authorized that exact
frozen target, but the original Ontology 006 executable still contains an
older hard-coded execution prerequisite requiring Provider Qualification 006.

Qualification 006 is DEGRADED and did not authorize the successor.

Therefore the original executable cannot consume the later valid
authorization and would terminate before constructing the semantic provider.

The original Ontology 006 artifact set must not be patched.

## Repair strategy

Create a new repaired execution target:

`OIC-DEFINITION-ONTOLOGY-PREDICATE-FRAME-DISCRIMINATION-006R1`

The repair is restricted to the execution-authorization binding surface.

The semantic experiment itself must remain unchanged.

## Mandatory semantic invariance

O006R1 must preserve all 18 semantic requests from frozen O006 exactly.

Required:

- 18 requests;
- 9 paired comparisons;
- 9 baseline requests;
- 9 treatment requests;
- identical request order;
- identical specimen/run/arm assignment;
- identical system prompts;
- identical user prompts;
- identical response formats;
- identical temperatures;
- identical token budgets;
- identical treatment guidance;
- identical semantic decision rule;
- identical adjudicability rule;
- identical target endpoint;
- identical safety endpoint;
- identical transport recovery policy.

Every repaired request projection must be byte-identical to its corresponding
frozen O006 request projection.

## Authorization repair

The repaired executable must not rely on the obsolete Qualification 006 live
receipt.

It must require a future fresh:

`OIC-NVIDIA-PROVIDER-QUALIFICATION-008`

and must consume its **formally closed tracked execution result**, not merely
its live receipt.

Qualification 008 must itself bind the exact statically frozen O006R1 artifact
set.

## No authorization inheritance

Qualification 007 authorized original O006 only.

That authorization is not inherited by O006R1.

Q007 observations are not reused as Qualification 008 observations.

O006R1 therefore remains unauthorized until a future separately preregistered
Qualification 008 is statically frozen, executed exactly once, formally closed
`QUALIFIED`, and verified.

## Frozen predecessor preservation

Original Ontology 006:

- remains unchanged;
- remains unexecuted;
- remains an immutable historical artifact;
- is not silently replaced or rewritten.

Qualification 006 receipt:

- remains unchanged.

## Current state

- provider calls: ZERO
- model calls: ZERO
- network calls: ZERO
- O006R1 implementation: NOT YET CREATED
- Qualification 008: NOT CREATED
- O006R1 execution: NOT AUTHORIZED
- semantic hypothesis: NOT EVALUATED
- architecture change: NOT AUTHORIZED

## Next activity

Implement O006R1 offline, prove exact semantic-request equivalence against
frozen O006, run locked tests, and statically freeze the repaired target.

Do not create Qualification 008 until that static freeze is independently
verified.
