# OIC NVIDIA Provider Qualification 009 — Preregistration

Status: **PREREGISTERED / NOT IMPLEMENTED / NOT EXECUTED**

## Purpose

Freshly qualify the NVIDIA hosted inference path immediately before the exact
statically frozen Ontology 007 syntactic predicate-carrier discrimination
experiment.

This is a provider-path gate only.

It evaluates no semantic hypothesis.

## Exact semantic successor

Qualification 009 is bound only to:

`OIC-DEFINITION-ONTOLOGY-SYNTACTIC-PREDICATE-CARRIER-DISCRIMINATION-007`

The bound descriptor includes the exact:

- preregistration commit;
- plan;
- preregistration;
- treatment binding;
- preregistration freeze;
- static freeze;
- instrument;
- contract test;
- 18-request materialization;
- provider adapter;
- frozen O006 source instrument;
- frozen O006 source materialization;
- request and pair counts.

Any target drift invalidates authorization.

## Provider probes

Exactly three frozen provider probes:

1. `BASIC_TEXT`
2. `JSON_MODE`
3. `PRODUCTION_TOKEN_RESERVATION`

Probe semantics remain bound to:

`262445c71ca34f41dd9d173a978ebcaa7bd71df2f313f0c9b090b9fd4a8925d1`

## Transport boundary

- timeout: 60 seconds
- latency headroom: 45 seconds
- retries: ZERO
- replacement probes: FORBIDDEN
- pacing: 4 seconds

## Decision rule

`QUALIFIED` requires exactly 3 terminal observations, all accepted,
all marker-valid, all <=45 seconds.

`DEGRADED` requires all 3 accepted and marker-valid, with one or more
latencies >45 seconds.

`NOT_QUALIFIED` includes any provider error, timeout, response mismatch,
invalid marker, missing probe, or incomplete execution.

## Formal-closure boundary

A live `QUALIFIED` disposition does not authorize Ontology 007.

Only the tracked formally closed Qualification 009 result may authorize it.

That result must be `CLOSED_QUALIFIED` and bind the exact frozen Ontology 007
descriptor.

## Non-inheritance

No Q008 observation is reused.

No Q008 authorization is inherited.

No Recovery Stability observation is reused.

Q009 is a fresh provider gate for Ontology 007.

## Current state

- provider calls: ZERO
- model calls: ZERO
- network calls: ZERO
- live Q009: NOT EXECUTED
- Q009 formally closed: FALSE
- Q009 qualified: FALSE
- Ontology 007 authorized: FALSE
- Ontology 007 executed: FALSE

## Claim ceiling

Provider-path qualification only.

No semantic finding, syntactic predicate-carrier result, production
interpretation rule, canonical institutional meaning, Institutional IR,
architecture change, cross-provider reliability, or independent validation
is established.

## Next activity

Implement, test, materialize and statically freeze Q009 offline.

Do not execute Q009 live.

Do not execute Ontology 007.
