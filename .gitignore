# Python build and cache artifacts
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Virtual environments
.venv/
venv/

# Tool caches
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Coverage output
.coverage
.coverage.*
coverage.xml
htmlcov/
junit.xml

# Local environment files. `.env.example` is checked in; a real `.env` is not.
.env
.env.local

# Canada procurement preflight: local-only downloads and metadata receipts.
.local/canada-preflight-quarantine/
.local/canada-preflight-receipts/

# macOS finder metadata
.DS_Store

# Canada corpus rights evidence and internal-only frozen bytes (never committed).
.local/canada-rights-evidence/
.local/canada-freeze-internal/
